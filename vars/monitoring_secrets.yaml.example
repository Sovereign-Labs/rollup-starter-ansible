---
# Monitoring Secrets - DO NOT COMMIT
# Copy this file to monitoring_secrets.yaml and fill in your actual values
# Make sure monitoring_secrets.yaml is in .gitignore

# ============================================
# Grafana Cloud Tokens
# ============================================

# InfluxDB token for metrics collection
# Used by Telegraf to send metrics to Grafana Cloud InfluxDB
influxdb_token: "YOUR_INFLUXDB_TOKEN_HERE"

# Grafana Loki token for log aggregation
# Used by Grafana Alloy to send logs to Grafana Cloud Loki
grafana_loki_token: "YOUR_LOKI_TOKEN_HERE"

# Grafana Tempo token for distributed tracing
# Used by Grafana Alloy to send traces to Grafana Cloud Tempo
grafana_tempo_token: "YOUR_TEMPO_TOKEN_HERE"

# ============================================
# How to Get These Values
# ============================================

# 1. Sign up for Grafana Cloud (free tier available):
#    https://grafana.com/products/cloud/

# 2. Create a stack in your preferred region

# 3. Generate API tokens:
#    - Go to your stack → Connections → Add new connection
#    - For InfluxDB: Create a token with write permissions
#    - For Loki: Create a token with logs:write permissions
#    - For Tempo: Create a token with traces:write permissions

# 4. Update the corresponding endpoint URLs in:
#    roles/common/defaults/main.yaml:
#    - influxdb_url
#    - loki_url
#    - tempo_url

# ============================================
# Security Notes
# ============================================

# - NEVER commit this file to git
# - Verify vars/monitoring_secrets.yaml is in .gitignore
# - Rotate tokens regularly
# - Use different tokens for dev/staging/production environments
# - Keep backups of your tokens in a secure password manager

# ============================================
# Optional: Disable Monitoring
# ============================================

# If you don't want to set up monitoring, you can:
# 1. Set empty strings for all tokens (will cause telegraf/alloy to fail silently)
# 2. Or skip the 'c' (common) switch when running the playbook to avoid setting up monitoring
