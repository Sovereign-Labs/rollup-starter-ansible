# Block requests from countries listed in $geoip_country_blocked (set up in
# conf.d/geoip-setup.conf). When GeoIP is disabled, $geoip_country_blocked is
# always 0, so this file is a no-op — safe to include unconditionally.
if ($geoip_country_blocked) {
    return 403;
}
