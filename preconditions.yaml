---
# Preconditions - Display configuration info
# Note: DA configuration variables are now loaded by the rollup role

- name: Display DA configuration
  debug:
    msg: "Using {{ data_availability_role }} DA"
  run_once: true
  when: "'r' in switches"
