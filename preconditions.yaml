---
# Preconditions - Load DA configuration variables
- include_vars: "roles/data-availability/{{ da_role }}/defaults/main.yaml"

# Display configuration info
- name: Display DA configuration
  debug:
    msg: "Using {{ da_role }} DA with start height: {{ da_start_height }}"
  run_once: true
