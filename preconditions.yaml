---
# Preconditions - Load DA configuration variables
- include_vars: "roles/data-availability/{{ data_availability_role }}/defaults/main.yaml"

# Display configuration info
- name: Display DA configuration
  debug:
    msg: "Using {{ data_availability_role }} DA with start height: {{ da_start_height }}"
  run_once: true
